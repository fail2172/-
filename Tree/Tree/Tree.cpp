#include <iostream>
#include "Header.h"

int main()
{
    Tree first_tree;
    int a = 5;
    int b = 8;
    int c = 3;
    int d = 5;
    first_tree.Insert(&a, first_tree.root_);
    first_tree.Insert(&b, first_tree.root_);
    first_tree.Insert(&c, first_tree.root_);
    first_tree.Insert(&d, first_tree.root_);



}
